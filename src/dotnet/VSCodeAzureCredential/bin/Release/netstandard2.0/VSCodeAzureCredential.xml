<?xml version="1.0"?>
<doc>
    <assembly>
        <name>VSCodeAzureCredential</name>
    </assembly>
    <members>
        <member name="T:VSCodeAzureCredential.VSCodeCredential">
            <summary>
            A <see cref="T:Azure.Core.TokenCredential"/> that obtains Azure access tokens from the
            VS Code "Azure Service Authentication" extension via its local loopback
            token broker.
            </summary>
            <remarks>
            Place this credential first in a <c>ChainedTokenCredential</c> so it is
            used when running tests inside VS Code, and falls through to other
            credentials in CI or when the extension isn't running.
            </remarks>
        </member>
    </members>
</doc>
